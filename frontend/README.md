# SmartPlot Frontend

This project currently uses plain HTML, CSS, and JavaScript in `public/` (not React/Vite). The backend Express server serves these files. Start the backend using its README instructions.
