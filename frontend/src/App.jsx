import './App.css';

export default function App() {
  return (
    <main>
      <h1>SmartPlot</h1>
      <p>The current working frontend is served from <code>public/index.html</code>.</p>
      <p>This React entry is a scaffold for a future migration; the existing HTML/CSS/JS app is preserved.</p>
    </main>
  );
}
