SmartPlot src directory

This folder provides the requested React-style organization (api, assets, components, pages, App.jsx, App.css, index.css, main.jsx).
The currently functional SmartPlot frontend remains in ../public because the supplied project is a vanilla HTML/CSS/JavaScript app, not a configured React/Vite app. The React files are a migration scaffold and are not wired into the current runtime.
